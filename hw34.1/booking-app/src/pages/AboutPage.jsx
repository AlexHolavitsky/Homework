import React from 'react';
import { Typography } from '@mui/material';

const AboutPage = () => (
  <div>
    <Typography variant="h4">About Us</Typography>
    <Typography>
      This is a booking application where you can find hotels by destination.
    </Typography>
  </div>
);

export default AboutPage;
